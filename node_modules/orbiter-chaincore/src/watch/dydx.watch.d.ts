import { Address, ITransaction, QueryTxFilterDydx, replayBlockCallback } from "../types";
import AbstractWatch from "./base.watch";
export declare class DydxWatch extends AbstractWatch {
    minConfirmations: number;
    getApiFilter(address: Address): Promise<QueryTxFilterDydx>;
    apiWatchNewTransaction(address: Address): Promise<Array<ITransaction>>;
    replayBlock(_start: number, _end: number, _changeBlock?: replayBlockCallback): Promise<{
        start: number;
        end: number;
    }>;
}
