import { IChainConfig, QueryTransactionsResponse, QueryTxFilter } from "../types";
import EtherscanClient from "../utils/etherscan";
import { EVMChain } from "./evm-chain.service";
export declare abstract class EvmExplorerService extends EVMChain {
    readonly chainConfig: IChainConfig;
    apiClient: EtherscanClient;
    constructor(chainConfig: IChainConfig);
    getTransactions(address: string, filter?: Partial<QueryTxFilter>): Promise<QueryTransactionsResponse>;
    getTokenTransactions(address: string, tokenAddress: string | null, filter?: Partial<QueryTxFilter>): Promise<QueryTransactionsResponse>;
    getInternalTransactions(address: string, filter?: Partial<QueryTxFilter>): Promise<QueryTransactionsResponse>;
}
