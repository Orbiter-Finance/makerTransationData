export default class Subgraph {
    private readonly endpoint;
    constructor(endpoint: string);
    query(graphqlQuery: any): Promise<any>;
    getLP(_id?: string): Promise<any>;
    getEffectLP(_id: string): Promise<void>;
}
