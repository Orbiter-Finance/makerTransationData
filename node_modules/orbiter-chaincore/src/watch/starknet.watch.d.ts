import { Starknet } from "../chain/starknet.service";
import { AddressMapTransactions, QueryTxFilter, replayBlockCallback } from "../types";
import BaseWatch from "./base.watch";
export declare class StarknetWatch extends BaseWatch {
    readonly chain: Starknet;
    minConfirmations: number;
    constructor(chain: Starknet);
    getApiFilter(_address: string): Promise<QueryTxFilter>;
    replayBlockTransaction(hashOrTx: string | any): Promise<AddressMapTransactions>;
    replayByBlock(start: string): Promise<AddressMapTransactions | undefined>;
    replayBlock(start: number, end: number, changeBlock?: replayBlockCallback): Promise<{
        start: number;
        end: number;
    }>;
}
