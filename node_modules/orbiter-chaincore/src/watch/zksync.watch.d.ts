import { Address, ITransaction, QueryTxFilterZKSync, replayBlockCallback } from "../types";
import AbstractWatch from "./base.watch";
export declare class ZKSyncWatch extends AbstractWatch {
    minConfirmations: number;
    getApiFilter(address: Address): Promise<QueryTxFilterZKSync>;
    apiWatchNewTransaction(address: Address): Promise<Array<ITransaction>>;
    replayBlock(_start: number, _end: number, _changeBlock?: replayBlockCallback): Promise<{
        start: number;
        end: number;
    }>;
}
