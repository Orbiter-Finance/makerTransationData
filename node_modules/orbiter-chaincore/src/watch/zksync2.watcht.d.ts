import { Address, ITransaction, QueryTxFilterEther } from "../types";
import EVMWatchBase from "./evm.watch";
export declare class ZKSync2Watch extends EVMWatchBase {
    readonly minConfirmations: number;
    private readonly feeAddress;
    getApiFilter(address: Address): Promise<QueryTxFilterEther>;
    protected pushBefore(address: Address, txList: Array<ITransaction>): Promise<{
        address: string;
        txList: import("../types").Transaction[];
    }>;
    apiWatchNewTransaction(address: Address): Promise<Array<ITransaction>>;
}
