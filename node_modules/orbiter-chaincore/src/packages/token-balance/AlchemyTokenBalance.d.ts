import { BalanceTokenItem } from "../../types";
import { ChainServiceTokenBalance } from "./ChainServiceTokenBalance";
export declare class AlchemyTokenBalance extends ChainServiceTokenBalance {
    protected readonly chainId: string;
    private alchemy;
    constructor(chainId: string);
    getHoldTokens(address: string): Promise<Array<BalanceTokenItem>>;
}
