import { Address, ITransaction, QueryTxFilterMetis } from "../types";
import EVMWatchBase from "./evm.watch";
export declare class MetisWatch extends EVMWatchBase {
    readonly minConfirmations: number;
    getApiFilter(address: Address): Promise<QueryTxFilterMetis>;
    apiWatchNewTransaction(address: Address): Promise<Array<ITransaction>>;
}
