import { Address, QueryTxFilterIMX, ITransaction, replayBlockCallback } from "../types";
import BaseWatch from "./base.watch";
export declare class ImmutableXWatch extends BaseWatch {
    minConfirmations: number;
    getApiFilter(address: Address): Promise<QueryTxFilterIMX>;
    apiWatchNewTransaction(address: Address): Promise<Array<ITransaction>>;
    replayBlock(_start: number, _end: number, _changeBlock?: replayBlockCallback): Promise<{
        start: number;
        end: number;
    }>;
}
