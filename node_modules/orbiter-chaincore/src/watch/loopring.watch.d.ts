import { Address, ITransaction, QueryTxFilterLoopring, replayBlockCallback } from "../types";
import AbstractWatch from "./base.watch";
export declare class LoopringWatch extends AbstractWatch {
    minConfirmations: number;
    getApiFilter(address: string): Promise<QueryTxFilterLoopring>;
    apiWatchNewTransaction(address: Address): Promise<Array<ITransaction>>;
    replayBlock(_start: number, _end: number, _changeBlock?: replayBlockCallback): Promise<{
        start: number;
        end: number;
    }>;
}
